package com.embarkx.e2eechatapp;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Minimal smoke test that verifies Spring can build the application context.
 */
@SpringBootTest
class E2EeChatappApplicationTests {

    @Test
    void contextLoads() {
        // If the application context fails to start, this test will fail automatically.
    }

}
